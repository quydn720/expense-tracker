import 'package:flutter/material.dart';

class StatsPage extends StatelessWidget {
  const StatsPage();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is Stats page'),
    );
  }
}
