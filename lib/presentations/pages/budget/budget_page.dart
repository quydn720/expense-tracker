import 'package:flutter/material.dart';

class BudgetPage extends StatelessWidget {
  const BudgetPage();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is Budget Page'),
    );
  }
}
